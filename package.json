{
  "name": "batch-promises-with-delay",
  "version": "1.0.0",
  "description": "Promise batching for unreliable api calls that have rate limits",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha './**/*.spec.js' --exit",
    "pre-commit": "npm test",
    "prepublish": "npm run test"
  },
  "author": "joeltok",
  "license": "MIT",
  "keywords": [
    "promise",
    "batch",
    "delay",
    "retry",
    "retries"
  ],
  "devDependencies": {
    "git-hooks": "^1.1.10",
    "mocha": "^5.2.0",
    "pre-commit": "^1.2.2",
    "sinon": "^7.2.3"
  }
}
